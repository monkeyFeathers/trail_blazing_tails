<h1>Listing admin_users</h1>

<table>
  <thead>
    <tr>
      <th>Fisrt name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Password digest</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_users.each do |admin_user| %>
      <tr>
        <td><%= admin_user.fisrt_name %></td>
        <td><%= admin_user.last_name %></td>
        <td><%= admin_user.email %></td>
        <td><%= admin_user.password_digest %></td>
        <td><%= link_to 'Show', admin_user %></td>
        <td><%= link_to 'Edit', edit_admin_user_path(admin_user) %></td>
        <td><%= link_to 'Destroy', admin_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fake admin user', new_admin_user_path %>
